# Core pipeline
GEOparse
pandas
numpy
scikit-learn
mygene
inmoose

# Landscape (risk model + surface)
lifelines
scipy
umap-learn  # optional — only for embed(method="umap")

# Claude (metadata parse + verify)
anthropic

# Web layer (Days 4-5)
fastapi
uvicorn[standard]
python-multipart

# MCP layer (Day 4)
mcp

# Tests
pytest
